import { Loader } from ".";

const Loading = () => {
  return (
    <div className="max-w min-h relative flex items-center justify-center">
      <Loader />
    </div>
  );
};

export default Loading;
